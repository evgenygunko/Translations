# npm configuration
# This file helps configure npm for Renovate and CI/CD pipelines

# Use npm registry
registry=https://registry.npmjs.org/

# Use yarn as package manager (as specified in package.json)
# This ensures consistency across environments

# Uncomment if you need authentication for private npm packages
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Uncomment for Azure DevOps Artifacts (if using private feed)
# registry=https://pkgs.dev.azure.com/evgenyg/_packaging/your-feed-name/npm/registry/
# always-auth=true
