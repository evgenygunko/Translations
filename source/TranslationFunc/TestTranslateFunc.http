#@service=azuretranslator
@service=openai

### Translate from Danish to English + Russian

POST {{HostAddress}}/api/Translate?code={{code}}&service={{service}}
Content-Type: application/json

{
  "HeadWord": "såsom",
  "Meanings": [
      "bruges til angivelse af et eller flere eksempler på noget",
      "bruges som indledning til en ledsætning der angiver en begrundelse"
  ],
  "SourceLanguage": "da",
  "DestinationLanguages": [
    "en",
    "ru"
  ]
}

> {%

  // Assert status code
  client.test('When sending a word for translation, we expect to get json back', () => {
    client.assert(response.status === 200, "Response status is not 200");
    client.assert(response.body[0]["Language"] === "ru");
    client.assert(response.body[0]["HeadWord"] === "конечность");
    client.assert(response.body[1]["Language"] === "en");
    client.assert(response.body[1]["HeadWord"] === "lim");
  });
%}



### Translate from Danish to English + Russian, where meanings are empty

POST {{HostAddress}}/api/Translate?code={{code}}
Content-Type: application/json

{
  "HeadWord": "såsom",
  "Meanings": [],
  "SourceLanguage": "da",
  "DestinationLanguages": [
    "en",
    "ru"
  ]
}

> {%

  // Assert status code
  client.test('When smeanings are empty, we expect to get json back', () => {
    client.assert(response.status === 200, "Response status is not 200");
    client.assert(response.body[0]["Language"] === "en");
    client.assert(response.body[0]["HeadWord"] === "such as");
    client.assert(response.body[1]["Language"] === "ru");
    client.assert(response.body[1]["HeadWord"] === "как");
  });
%}



### BadRequest - SourceLanguage is null

POST {{HostAddress}}/api/Translate?code={{code}}
Content-Type: application/json

{
  "HeadWord": "såsom",
  "Meanings": [
      "bruges til angivelse af et eller flere eksempler på noget",
      "bruges som indledning til en ledsætning der angiver en begrundelse"
  ],
  "SourceLanguage": "",
  "DestinationLanguages": [
    "en",
    "ru"
  ]
}

> {%

  // Assert status code
  client.test('When SourceLanguage is null, we expect to get an error', () => {
    client.assert(response.status === 500, "Response status is not 500");
  });
%}



### BadRequest - HeadWord is null

POST {{HostAddress}}/api/Translate?code={{code}}
Content-Type: application/json

{
  "HeadWord": "",
  "Meanings": [
      "bruges til angivelse af et eller flere eksempler på noget",
      "bruges som indledning til en ledsætning der angiver en begrundelse"
  ],
  "SourceLanguage": "Danish",
  "DestinationLanguages": [
    "en",
    "ru"
  ]
}

> {%

  // Assert status code
  client.test('When HeadWord is null, we expect to get an error', () => {
    client.assert(response.status === 500, "Response status is not 500");
  });
%}



### BadRequest - DestinationLanguages has too many elements

POST {{HostAddress}}/api/Translate?code={{code}}
Content-Type: application/json

{
  "HeadWord": "såsom",
  "Meanings": [
      "bruges til angivelse af et eller flere eksempler på noget",
      "bruges som indledning til en ledsætning der angiver en begrundelse"
  ],
  "SourceLanguage": "Danish",
  "DestinationLanguages": [
    "en",
    "ru",
    "es"
  ]
}

> {%

  // Assert status code
  client.test('When DestinationLanguages has too many elements, we expect to get an error', () => {
    client.assert(response.status === 500, "Response status is not 500");
  });
%}



### Test connection to Azure Translator Service

https://api.cognitive.microsofttranslator.com/languages?api-version=3.0

###
